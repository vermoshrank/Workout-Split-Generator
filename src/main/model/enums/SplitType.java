package model.enums;

// represents the different split types/days for a workout plan
public enum SplitType {
    PUSH,
    PULL,
    LEGS,
    UPPER,
    LOWER,
    FULL_BODY,
    PUSH_PULL_LEGS,
    PPL_UL,
    UPPER_LOWER
}
